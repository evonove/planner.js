{{! List of computed styles }}

.column > div:not(:first-child) {
    height: {{rowHeight}}px;
}

{{#if textAlignment}}
    .planner-timeslots > div {
        padding-top: {{topPadding}}px;
    }
{{/if}}

.planner-timeslots > div:not(:first-child) {
    padding-top: {{rowPadding}}px;
}
